'use strict';

const moment = require('moment');

let ValidationError = require('../error');

module.exports = {
  // (subject, param={}, opts) {
  //   if (typeof) return new ValidationError('', param);
  //   else return subject;
  // },
};
